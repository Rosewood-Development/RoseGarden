package dev.rosewood.menuexamples.gui.model;

// Your title object that holds the data for each title
public record Title(String id,
                    String title,
                    String type) { }
